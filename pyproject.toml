[tool.black]
line-length = 100
target-version = ['py310']
skip-string-normalization = true
per-file-ignores = [
    # Combine all ignores for 'tests/*' into this one string
    tests/*: F403, F405, S101, F841, F811
]