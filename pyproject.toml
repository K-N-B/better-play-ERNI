[tool.black]
line-length = 100
target-version = ['py310']
skip-string-normalization = true


[tool.flake8]
exclude = [
    ".git",
    "__pycache__",
    ".tox",
    ".venv",
    "build",
    "dist",
    "*/tests.py",  # Ignores files named tests.py
    "*/tests/"
]